{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "conda run --no-capture-output -n face-ai python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 --env-file ../../.env"
  }
}
