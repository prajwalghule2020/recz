FROM node:22-alpine

WORKDIR /app

# Install prisma CLI
RUN npm install -g prisma

# Copy the shared DB schema and migrations
COPY packages/db/prisma ./prisma

# Default command: run migrations
CMD ["prisma", "migrate", "deploy", "--schema=./prisma/schema.prisma"]
